<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa; /* Light gray color */
        }

        .card {
            background-color: #111;
            color: #fff;
        }
    </style>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NutriNatureFruit</title>
</head>
<body>
    <div class="container-fluid p-5"> <!-- Added container-fluid class for full width and p-5 for padding -->
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title text-center">Forgot Password</h5>
                        <form action="/resetPasswordEmail" method="post">
                            <!-- Email input -->
                            <div class="mb-3">
                                <label for="email" class="form-label">Email address</label>
                                <input type="email" name="email" id="email" class="form-control" required>
                            </div>
                        
                            <!-- Submit button -->
                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary">Send OTP</button>

                                <% if(typeof message!=='undefined'){%>
                                    <p><%= message %></p>
                                    <% }%>
                            </div>

                        
                            <!-- Back to login link -->
                            <p class="text-center mt-3">Remembered your password? <a href="/login">Back to login</a></p>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
